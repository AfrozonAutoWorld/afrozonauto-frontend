import { Contact } from '@/views/Contact';

export const metadata = {
  title: 'Contact us | Afrozon AutoGlobal',
  description: 'Reach Afrozon support for help with orders, payments, and listings.',
};

export default function ContactPage() {
  return <Contact />;
}
